<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<document type="freeswitch/xml">
  <section name="directory">
    <domain name="verto">
      <user id="{{ client.client_id }}">
        <params>
          <param name="jsonrpc-allowed-methods" value="echo,verto.invite,verto.attach"/>
          <param name="password" value="{{ client.password }}"/>
        </params>
      </user>
    </domain>
  </section>
</document>
